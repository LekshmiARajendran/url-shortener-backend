spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/urlshortener_test
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

# Tell Spring not to replace this with an embedded DB
test:
  database:
    replace: none
